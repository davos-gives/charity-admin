<div class="w-9/10 bg-white rounded-xl p-8">
  <div class="flex align-bottom mb-2 pb-2 mt-8">
    <h2 class="flex-1 text-grey-darker text-lg pb-4 font-bold align-middle self-center ml-8 flex flex-wrap items-center opacity-50"><span class="uppercase">Active Donations</span> <img src="/img/icons/ChevronDown.svg" class="w-4 mx-4 rotate-270">
 Edit Donation</h2>
  </div>
  <div class="flex align-bottom mb-2 pb-2">
    <h1 class="flex-1 text-grey-darker text-2xl pb-4 font-normal align-middle self-center ml-8 flex flex-wrap items-center mt-8">Edit Donation</h1>
  </div>

  <div class="w-5/6 mx-auto">

    <OngoingDonationForm @donation={{model}}/>

    <div class="flex flex-wrap justify-between items-center">
      <div class="w-1/3">
        <img class="" src="/img/change-ongoing-donation.png" />
      </div>
      <div class="w-2/3 pl-4">
        <p class="text-purple font-bold text-lg">Donor requested changes?</p>
        <p class="text-grey-darker leading-normal my-2">Make sure this donor requested that you make these edits. You're about to make changes to an active ongoing donation, which will modify any upcoming payments.</p>
        {{#if approvedModification}}
          <div class="flex flex-wrap text-grey-darker my-8 items-center" {{action "toggleApproved"}}><span class="text-sm bg-purple-dark w-6 h-6 flex items-center justify-center rounded-full uppercase mr-8"><img src="/img/icons/CheckWhite.svg" class="w-full"></span> Yes, i'm honouring my donors wish.</div>
        {{else}}
          <div class="flex flex-wrap text-grey-darker my-8 items-center" {{action "toggleApproved"}}><span class="text-sm border-grey-dark border-2 w-6 h-6 flex items-center justify-center rounded-full uppercase mr-8"></span> Yes, i'm honouring my donors wish.</div>
        {{/if}}

      </div>
    </div>
    <div class="flex flex-wrap float-right py-8 mb-8">
      {{#if approvedModification}}
      <button class="rounded-full bg-purple text-white font-thin py-2 px-4 rounded-full block z-10 hover:shadow-lg capitalize mr-4">
        Save
      </button>
      {{else}}
      <button class="rounded-full bg-purple text-white font-thin py-2 px-4 rounded-full block z-10 hover:shadow-lg capitalize mr-4 opacity-50 cursor-not-allowed" disabled>
        Save
      </button>
      {{/if}}
      {{#link-to "donors.show" model.donor class="no-underline"}}
        <button class="rounded-full border border-purple-buttons text-purple-buttons font-thin py-2 px-4 rounded-full block z-10 hover:shadow-lg">Cancel</button>
      {{/link-to}}
  </div>
    <div>
    </div>
  </div>
</div>
